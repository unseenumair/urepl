name: Publish to PyPI

on:
  push:
    tags:
      - 'v*' # This triggers the automation ONLY when you push a version tag like v1.0.2

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # This is required for Trusted Publishing security

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        pip install build

    - name: Build package distribution
      run: python -m build

    - name: Publish package to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
